export { hooks, setHookCModasofack };

var hookCModasofack;

function hooks () {
    return hookCModasofack.apply(null, arguments);
}

// This is done to register the method called with moment()
// without creating circular dependencies.
function setHookCModasofack (cModasofack) {
    hookCModasofack = cModasofack;
}
